# Standards

Use strict TypeScript. No explicit any, double assertions, unchecked type assertions, or ts-ignore. Unknown belongs at validation entrances. Test observable behavior through submit, view, snapshot and restore. Rules are trusted code, external commands and snapshots are not trusted. Never expose authoritative mutable state. No unrequested generic registries. Document experimental limitations honestly. Run npm run check before commits.
